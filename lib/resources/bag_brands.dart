import 'package:flutter/material.dart';

class BagBrand extends StatefulWidget {
  const BagBrand({Key? key}) : super(key: key);

  @override
  State<BagBrand> createState() => _BagBrandState();
}

class _BagBrandState extends State<BagBrand> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text("Apis are not received yet..")),
    );
  }
}
