import 'package:flutter/material.dart';

class WalletBrand extends StatefulWidget {
  const WalletBrand({Key? key}) : super(key: key);

  @override
  State<WalletBrand> createState() => _WalletBrandState();
}

class _WalletBrandState extends State<WalletBrand> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text("Apis are not received yet..")),
    );
  }
}
